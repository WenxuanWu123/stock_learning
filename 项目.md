# stock_learning 项目宪法

> 本文件是本项目所有 AI 协作轮次的强制执行规则。任何一轮改动违反本文件，一律打回重做。

## 项目定位

A 股投资互动教学站。**自用为主**（唯一用户：站长本人），同时开源到 Gitee/GitHub。
目标：把股市概念、宏观经济学基础、技术分析指标、看盘技巧讲清楚，并用大量实例测试巩固。

## 范围（全量）

- 四大内容模块，开工顺序固定：
  1. `concepts` 股市概念（先做，用于跑通产线）
  2. `indicators` 技术指标
  3. `macro` 宏观经济学基础
  4. `trading` 看盘技巧（最后做，以 B 型关卡为主载体）
- 三种玩法：
  - **A 闯关刷题**：Markdown 课文 + JSON 题库（每章 10 题，含答错解析）
  - **B K线实战演练**：真实历史行情，在决策点停下让用户决策（买/卖/观望/止损位），随后揭示走势与系统打分理由
  - **C 参数实验场**：交互式图表调参数看指标变化（MACD/KDJ 等，随 indicators 模块做）
- 错题本、答题记录、成绩统计全部入库
- 特色内容：
  - 「我的操作 vs V5 信号」对照复盘（素材：站长本人交割单）
  - 个股反面教材快照（从站长亏损票中选：云计算、卫星ETF、机器人ETF、芯片、有色、黄金、比亚迪、京东方A、中国船舶、立讯精密等）

## 质量铁律（最高优先级）

1. **数字必须由脚本算出**。课文、题目、关卡解析中的一切具体数字（指标值、涨跌幅、日期、买卖点价格）一律使用占位符 `{{calc:...}}`，由 pipeline 从数据库/冻结 CSV 计算后注入。**AI 手写任何具体数字 = bug，打回。**
2. **每章标注来源**。front-matter 必须有 `sources:` 字段（权威投教材料/教材/本仓库内文档）。纯概念性文字允许 AI 发挥，但也必须标注参考来源。
3. **数据库可一键重建**。`./rebuild.sh` 删除并重建 SQLite，全部内容来自 `content/` + `data/`（git 内文件），不允许依赖任何在线接口（akshare 等只用于一次性制作冻结 CSV）。
4. **行情数据以冻结 CSV 为准**，存于 `data/` 并随 git 分发。初始数据来自 `/home/wwx121113/股市指标/`（上证日线、沪深300、中证500）。

## 技术形态

- 本地运行（这台机器：Linux，Python 3.13，Node 22）
- 后端：FastAPI + SQLite（独立数据库文件，不进 git）
- 前端：原生 JS SPA，**零构建步骤**（站长看不懂代码，不允许引入 npm 构建链）
- Python 依赖固定在本目录 `.venv`，`requirements.txt` 锁版本
- `./run.sh` 一条命令起站；`./rebuild.sh` 一条命令重建数据库
- 隐私文件（交割单截图等）放 `private/`，必须 gitignore，任何开源导出不得包含

## 内容文件格式

```
content/<module>/<NN>-<slug>/
  lesson.md      # 课文。YAML front-matter: title, order, sources[]
  quiz.json      # {"questions": [{"q","options"[4],"answer"(0-3),"explanation"}]}，每章 10 题
levels/<NN>-<slug>.json   # B 型关卡：symbol, data_file, decision_date, reveal_days, question, options, scoring
```

- 课文和题目中的数字一律写 `{{calc:...}}` 占位符（具体语法由 pipeline 实现并在本文件补充说明）
- 站长只编辑 `content/` 和 `levels/` 下的 .md/.json，不碰任何代码

## 模块验收关口

每个模块完成标准：产线脚本能无错注入全部数字 + 站长实际做题通关 + 抽查来源无误。过关才开下一模块。

## 协作模式

- 代码 100% AI 代工；站长出需求、做验收、编辑内容文件
- 每轮 AI 协作开工前必须读本文件

## calc 占位符语法（pipeline 已实现，内容代理照此书写）

课文 `lesson.md`、`quiz.json` 的题干/选项/解析、`levels/*.json` 的
question/options.text/options.feedback 中，一切具体数字一律写占位符，
seed 时由 `app/calc.py` 从行情表计算后注入，替换记录存于 lessons.calc_log
/ levels.calc_log 供审计。**手写数字 = bug，打回。**

### 形式

```
{{calc:函数(参数...)|精度}}
```

- `|精度` 可省略，默认保留 2 位小数；`|0` 取整。
- 日期参数写 `YYYY-MM-DD`；若为非交易日，自动取**该日（含）之前最近一个交易日**。
- symbol 取值：`sh000001`（上证指数）/ `sh000300`（沪深300）/ `sh000905`（中证500）。

### 可用函数

| 函数 | 含义 | 示例 |
|---|---|---|
| `close(symbol, date)` | 收盘价 | `{{calc:close(sh000001, 2026-07-21)}}` |
| `open(symbol, date)` / `high(...)` / `low(...)` | 开/高/低价 | 同上 |
| `volume(symbol, date)` | 成交量（整数） | 同上 |
| `trade_date(symbol, date)` | 对齐后的实际交易日 | 用于正文先交代日期 |
| `chg_pct(symbol, start, end)` | 区间涨跌幅 %（两端各取最近交易日收盘） | `{{calc:chg_pct(sh000001, 2015-01-05, 2015-06-12)}}` |
| `ma(symbol, N, date)` | N 日均线值 | `{{calc:ma(sh000001, 250, 2026-07-21)}}` |
| `rsi(symbol, N, date)` | N 日 RSI（通达信 SMA 口径） | `{{calc:rsi(sh000001, 6, 2025-04-07)}}` |
| `bias(symbol, N, date)` | N 日乖离率 % | `{{calc:bias(sh000001, 20, 2025-04-07)}}` |
| `macd(symbol, date)` | MACD(12,26,9)，输出 `DIF=..，DEA=..，MACD=..` | — |
| `kdj(symbol, date)` | KDJ(9,3,3)，输出 `K=..，D=..，J=..` | — |
| `max_high(symbol, start, end)` | 区间最高价 | — |
| `min_low(symbol, start, end)` | 区间最低价 | — |
| `symbol_name(symbol)` | 标的中文名 | — |

### 书写纪律

1. 区间类表述先写 `trade_date` 对齐日期，再写数值，保证正文日期与数据一致。
2. 占位符报错（语法错/函数不存在/日期无数据）会让 `rebuild.sh` 直接失败并指出
   位置——这是有意设计：宁可建不出来，也不许错数字上线。
3. 新增函数必须改 `app/calc.py` 的 `FUNCS` 表并更新本节文档。
